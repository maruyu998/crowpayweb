# Todo
`secret.js` ファイルをこのディレクトリに作成

```
module.exports = {
    session_secret: "~~~",
    hash_salt: "~~~~",
    basic_auth_username: "~~~",
    basic_auth_password: "~~~",
    mongo_path: "mongodb://localhost:27017/crowpayなど"
}
```